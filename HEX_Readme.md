HEX-значение **0x42** переводится в десятичное число так:

### Пошагово

Hex **0x42** =
$4 \times 16^1 + 2 \times 16^0$ = $4 \times 16 + 2 \times 1$ = $64 + 2$ = **66**

### Итог

* **0x42 (hex)** = **66 (decimal)**
* **0x42 (hex)** = **0100 0010 (binary)**

-----------

HEX **0xA7** конвертируется так:

### Пошагово

* **A** = 10
* **7** = 7

$10 \times 16^1 + 7 \times 16^0 = 160 + 7 = 167$

### Итог

* **0xA7 (hex)** = **167 (decimal)**
* **0xA7 (hex)** = **1010 0111 (binary)**


------
🧪 Парсинг HEX-строк

    int value = std::stoi("A7", nullptr, 16); // --> 167

------
🧨 HEX и endianness. 
В памяти:

    Little-endian: 78 56 34 12
    Big-endian: 12 34 56 78

-----
🧨 Вариация с аналога htons() с использованием 'std::byteswap(v)'

    template <std::integral T>
    constexpr T toBigEndian(T v)
    {
        static_assert(std::is_integral_v<T>);
        if constexpr (std::endian::native == std::endian::little)
            return std::byteswap(v);
        else
            return v;
    }

    std::cout << toBigEndian<uint16_t>(443) << std::endl;

-----



Ниже — **практичный cheat-sheet часто используемых HEX-значений**, именно тех, которые **реально встречаются в C/C++**, embedded, протоколах и low-level коде.



## 🔢 Базовые значения (байт)

| HEX    | Dec | Bin         | Комментарий |
| ------ | --- | ----------- | ----------- |
| `0x00` | 0   | `0000 0000` | ноль, reset |
| `0x01` | 1   | `0000 0001` | младший бит |
| `0x02` | 2   | `0000 0010` | бит 1       |
| `0x04` | 4   | `0000 0100` | бит 2       |
| `0x08` | 8   | `0000 1000` | бит 3       |
| `0x10` | 16  | `0001 0000` | бит 4       |
| `0x20` | 32  | `0010 0000` | бит 5       |
| `0x40` | 64  | `0100 0000` | бит 6       |
| `0x80` | 128 | `1000 0000` | старший бит |

---

## 🧩 Маски и границы

| HEX      | Назначение              |
| -------- | ----------------------- |
| `0xFF`   | весь байт               |
| `0x0F`   | младшие 4 бита (nibble) |
| `0xF0`   | старшие 4 бита          |
| `0x7F`   | max int8 (127)          |
| `0x80`   | sign bit                |
| `0xFF00` | старший байт            |
| `0x00FF` | младший байт            |

---

## 📦 Часто используемые шаблоны

| HEX          | Где используется |
| ------------ | ---------------- |
| `0xAA`       | preamble, sync   |
| `0x55`       | инверсия 0xAA    |
| `0xA5`       | тестовый паттерн |
| `0x5A`       | тестовый паттерн |
| `0xDEADBEEF` | debug marker     |
| `0xCAFEBABE` | сигнатура        |

---

## 🌐 Сети и протоколы

| HEX      | Значение                |
| -------- | ----------------------- |
| `0x0800` | IPv4 (EtherType)        |
| `0x86DD` | IPv6                    |
| `0x0806` | ARP                     |
| `0x45`   | IPv4 header (v4, IHL=5) |
| `0xFF`   | broadcast               |

---

## 🔐 Спецзначения / маркеры

| HEX                  | Назначение             |
| -------------------- | ---------------------- |
| `0xFFFFFFFF`         | invalid / all bits set |
| `0x00000000`         | clear                  |
| `0x80000000`         | MSB flag               |
| `0xFFFFFFFFFFFFFFFF` | uint64 max             |

---

## 🧪 ASCII (часто путают)

| Символ | HEX    | Dec |
| ------ | ------ | --- |
| `'0'`  | `0x30` | 48  |
| `'9'`  | `0x39` | 57  |
| `'A'`  | `0x41` | 65  |
| `'F'`  | `0x46` | 70  |
| `'a'`  | `0x61` | 97  |
| `'f'`  | `0x66` | 102 |
| `'\n'` | `0x0A` | 10  |
| `'\r'` | `0x0D` | 13  |

---

## ⚠️ Классические ошибки

| HEX               | Ошибка                     |
| ----------------- | -------------------------- |
| `0xA7` в `int8_t` | станет отрицательным       |
| `0xFF` в `char`   | зависит от signed/unsigned |
| `0x80`            | знак у int8                |

---

## 🧠 Быстрые правила

* `0xFF` — «всё»
* `0x80` — «знак»
* `0x0F / 0xF0` — nibble
* `0xAA / 0x55` — синхронизация
* `0xDEADBEEF` — debug

---
